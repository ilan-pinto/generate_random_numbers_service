version: '3.8'
services:
        rest_service:
                build: .
                ports:
                        - "8080:8080"
                depends_on:
                        - mysql_database

        mysql_database:
                image: "mysql:latest" 
                environment:
                        - MYSQL_ROOT_PASSWORD="!Rainman1"
                ports:
                        - "3306:3306"
